import Button from "../components/typescript-in-react/Button";

const Ts = () => {
  return (
    <div className="w-40 m-16">
      <Button
        backgroundColor="red"
        fontSize={24}
        textColor="white"
        padding={[16, 32, 16, 32]}
      />
    </div>
  );
};

export default Ts;
